// IMPORTANTE: Este arquivo foi substituído pelo sistema multi-tenant híbrido
// Use src/lib/auth-hybrid.ts para a nova implementação
// Mantido para compatibilidade durante transição

// Re-export do sistema híbrido
export { authHybridConfig as authConfig } from './auth-hybrid'